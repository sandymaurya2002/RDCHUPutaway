name: 🏗️ Build Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-apk:
    name: Build Debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Node 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: ☕ Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🤖 Android SDK
        uses: android-actions/setup-android@v3

      - name: 📦 Android platform tools
        run: |
          sdkmanager "platforms;android-34" "build-tools;34.0.0" 2>/dev/null | grep -E "done|Installed" || true

      - name: 🏗️ react-native init scaffold
        env:
          CI: ""
        run: |
          npx --yes react-native@0.73.6 init RDCBuild \
            --version 0.73.6 \
            --directory /tmp/RDCBuild \
            --skip-install \
            --npm
          echo "✅ Scaffold done"

      - name: 🔀 Overlay our source files
        run: |
          S=/tmp/RDCBuild
          W=$GITHUB_WORKSPACE

          # JS source
          rm -rf $S/src
          cp -rf $W/src $S/src
          cp -f $W/App.js $W/index.js $W/app.json $W/babel.config.js $W/metro.config.js $W/package.json $S/

          # Android resources
          cp -f $W/android/app/src/main/AndroidManifest.xml \
                $S/android/app/src/main/AndroidManifest.xml
          cp -f $W/android/app/src/main/res/values/strings.xml \
                $S/android/app/src/main/res/values/strings.xml
          cp -f $W/android/app/src/main/res/values/styles.xml \
                $S/android/app/src/main/res/values/styles.xml

          # Kotlin - find scaffold's actual package dir dynamically
          KT_DIR=$(find $S/android -name "MainActivity.kt" | head -1 | xargs dirname)
          echo "Kotlin dir: $KT_DIR"
          PKG=$(echo $KT_DIR | sed 's|.*/java/||' | tr '/' '.')
          echo "Package: $PKG"
          cp -f $W/android/app/src/main/java/com/rdchuputaway/MainActivity.kt $KT_DIR/
          cp -f $W/android/app/src/main/java/com/rdchuputaway/MainApplication.kt $KT_DIR/
          sed -i "s/^package .*/package $PKG/" $KT_DIR/MainActivity.kt
          sed -i "s/^package .*/package $PKG/" $KT_DIR/MainApplication.kt

          # ✅ Override gradle files with our versions
          cp -f $W/android/build.gradle $S/android/build.gradle
          cp -f $W/android/gradle.properties $S/android/gradle.properties
          cp -f $W/android/app/build.gradle $S/android/app/build.gradle
          cp -f $W/android/app/proguard-rules.pro $S/android/app/proguard-rules.pro

          # ✅ Use Gradle 9.0.0
          cp -f $W/android/gradle/wrapper/gradle-wrapper.properties \
                $S/android/gradle/wrapper/gradle-wrapper.properties

          echo "=== gradle-wrapper.properties ==="
          cat $S/android/gradle/wrapper/gradle-wrapper.properties
          echo "=== settings.gradle ==="
          cat $S/android/settings.gradle 2>/dev/null || cat $S/android/settings.gradle.kts

      - name: 📦 npm install
        run: |
          cd /tmp/RDCBuild
          npm install --legacy-peer-deps
          echo "✅ $(ls node_modules | wc -l) packages"

      - name: ⬇️ Download gradle-wrapper.jar
        run: |
          curl -fsSL \
            "https://raw.githubusercontent.com/gradle/gradle/v8.6.0/gradle/wrapper/gradle-wrapper.jar" \
            -o /tmp/RDCBuild/android/gradle/wrapper/gradle-wrapper.jar
          ls -lh /tmp/RDCBuild/android/gradle/wrapper/gradle-wrapper.jar

      - name: 💾 Cache Gradle
        uses: actions/cache@v4
        with:
          path: ~/.gradle
          key: gradle-${{ runner.os }}-9-0-0-v1

      - name: 🔧 chmod gradlew
        run: chmod +x /tmp/RDCBuild/android/gradlew

      - name: 🏗️ Build APK
        run: |
          cd /tmp/RDCBuild/android
          ./gradlew assembleDebug \
            --no-daemon \
            --stacktrace \
            -Dorg.gradle.jvmargs="-Xmx4096m"
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
          JAVA_HOME: ${{ env.JAVA_HOME }}

      - name: 🔍 Find APK
        run: find /tmp/RDCBuild -name "*.apk" 2>/dev/null

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: RDCHUPutaway-debug-apk
          path: /tmp/RDCBuild/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
